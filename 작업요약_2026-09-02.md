# 2026-09-02 작업 요약

## 1. n8n 파이프라인 설정 변경
- `max_tokens` 32000 → **64000**으로 상향 (1차 생성/2차 검증 노드 모두)
- HTTP Request 노드 timeout 600000 → **900000**(15분)으로 상향
- 목적: 리포트 출력이 도중에 잘리는 것 방지

## 2. v4.0 지침 3회 적용 (로컬 파일 + n8n `Edit Fields` 노드 `guideline_text`)
로컬 파일: `프리미엄 종합 사주 · 인생 전략 리포트 분석 지침.txt`

1차 → 2차(세부 개정): 한글(한자) 순서 고정, 오행 5글자 자연어화(나무·불·흙·쇠·물), 자기소개 금지, 표-본문 정합성 등 세부 규칙 추가
3차(오늘 마지막): 1-6 필수 커버리지가 "현재 대운의 흐름"만 다루던 걸 **"대운의 전체 흐름(생애 전체 + 현재 대운 집중)"**으로 확장, **세부 트렌드 운세명 대응**(귀인운·재회운 등) 조항 추가, **분량 유지**(항목 생략 금지) 조항 추가, **용신·대운수는 명칭과 함께 설명** 조항 추가. 3-1 항목12 "인생 거시 흐름"도 "현재 대운 중심" → "대운 전체를 표로 훑고 현재/다음 대운만 깊게"로 확장.

**현재 n8n에 반영된 지침은 이 최신(3차) 버전이며, 로컬 파일과 완전히 동기화되어 있음.**

## 3. 병렬 생성 구조 실험 → 실패 → 원복 (중요)
- 사용자 제안으로 리포트를 A(1-6장)/B(7-11장)/C(12-18장) 3파트로 나눠 병렬 생성 → Merge 노드로 합침 → 기존 검증 노드로 넘기는 8노드 구조를 구축·테스트함.
- **결론: n8n은 캔버스에서 브랜치가 갈라져 있어도 하나의 실행(execution) 내부에서는 노드를 항상 하나씩 순차 처리한다.** 실제 실행 타임스탬프로 확인: A가 끝난 정확한 시각에 B가 시작, B가 끝난 시각에 C가 시작 — 전혀 동시 실행이 아니었음.
- 결과: 총 20분 44초 소요, 기존 순차 방식(12분 35초)보다 오히려 8분 더 느림. 프롬프트 캐싱 이득도 못 받고(3개가 동시에 콜드 스타트) 노드 오버헤드만 추가됨.
- **원래의 5노드 순차 구조(Webhook → If → Edit Fields → HTTP Request → HTTP Request1)로 완전히 복구 완료.** 복구 후 재테스트: 12분 52초로 정상 확인.
- **n8n 작업 시 유의점**: 브라우저 콘솔로 워크플로우를 직접 조작할 때 `pinia._s.get('workflowDocuments/{id}@latest')` 스토어의 `setNodeParameters`, `addConnection`, `removeConnection`, `addNode`, `removeNodeById`, `setConnections` 함수를 직접 호출하는 게 UI 드래그보다 훨씬 안정적. 단, 편집 후 반드시 **새로고침해서 서버에 실제 저장됐는지 재확인**할 것(로컬 상태와 서버 상태가 어긋난 채로 "발행됨"이라고 잘못 표시되는 경우가 있었음). Merge 노드는 `mode:"append"` (모든 입력을 기다림, 필드 매칭 불필요)가 이런 용도엔 정답.

## 4. 세로형 A4 리포트 미리보기 아티팩트
- `report_preview.html` (아티팩트 URL: `ef9113d1-232b-4fa7-92de-78d02b0132bb`) — 검증용 세로형 마크다운 리포트 뷰어. 여러 차례 테스트 결과를 갱신하며 사용.
- 현재는 "지침수정테스트" 고객(1990-01-01, 여성, 서울, 병(丙)일간)의 최종 검증본이 반영되어 있음.

## 5. 디자인 방향 결정: 가로형(16:9) 슬라이드 포맷
텍스트 리포트가 순차 방식으로는 약 20페이지 정도인데, 경쟁 서비스 대비 분량이 부족해 보인다는 문제의식에서 출발한 논의:

- **Canva API 연동은 채택 안 함** — 리포트당 API 비용·시간 추가, 개인용 도구에 과한 외부 의존성.
- **PDF는 세로형(A4)보다 가로형(16:9) 슬라이드가 낫다고 결론** — 가로 슬라이드는 한 페이지에 담기는 글자량이 자연히 줄어드는 포맷이라, 폰트를 억지로 안 키워도 페이지 수가 자연스럽게 늘어남. "컨설팅 보고서" 느낌도 더 잘 남.
- **AI 추가 비용 없음** — 이미 생성된 리포트 텍스트를 챕터/소제목 구조 그대로 슬라이드에 기계적으로 나눠 담는 방식. 오행/십성/대운 같은 수치 데이터도 이미 있는 JSON을 그대로 시각화. 리포트 한 건당 추가로 드는 AI 토큰 비용은 0원.
- **3페이지(사주 스냅샷)에 고객 입력정보도 함께 넣기로 결정** → 결과적으로 "고객 프로필" 페이지와 "사주 스냅샷" 페이지 2개로 분리.

## 6. 슬라이드 프로토타입 → 완성본 제작
1. **8장 프로토타입** (아티팩트: `eea51152-6ff6-4e9a-8585-cedd9f92bf8a`, "가로형 사주 리포트 시안"): 표지·목차·고객프로필·사주스냅샷·챕터디바이더·본문·데이터시각화(오행)·인용구 브레이크 8가지 슬라이드 유형 확인용. 본문 폰트 14.5px→17px 및 전체 타이포 비율(×1.17) 조정 반영.
2. **49페이지 완성본** (아티팩트: `3d7c4666-5912-4043-8ab6-263d60de35c8`, "사주 리포트 슬라이드 전체본"): 실제 검증된 리포트 텍스트(14개 장 전체)를 마크다운 파싱 → 챕터별 소제목 단위로 쪼개서 JS에 임베드 → **자동 페이지네이션 로직**(문단 단위로 높이를 측정해서 한 슬라이드(16:9, 1180px 기준)에 안 들어가면 다음 슬라이드로 자동 이월)으로 렌더링.
   - 구성: 표지·목차·프로필·스냅샷 4p + 14개 장(디바이더+본문, 자동분할로 총 29개 본문 슬라이드) + 데이터 시각화 2p(오행 막대그래프, 3개년 세운 카드) + 인용구 브레이크 2p = **총 49페이지**
   - 오버플로우 버그 발견 및 수정: 처음엔 "소제목 통째로" 단위로 페이지를 나눠서 콘텐츠가 슬라이드 밖으로 거의 2배 넘치는 문제가 있었음 → **문단(paragraph) 단위**로 잘게 쪼개 측정하도록 수정 → 검증 결과 49페이지 전부 오버플로우 0건, 평균 채움률 100%.
   - 로컬 파일: `slide_prototype.html`(8장 시안), `slide_full.html`(49페이지 완성본) — 둘 다 스크래치패드 임시 폴더에 있음(세션 종료 시 사라질 수 있으니 필요하면 아티팩트에서 다시 읽어올 것).

## 7. 현재 상태 / 다음 세션에서 이어갈 것
- **n8n 파이프라인**: 정상 작동 중인 5노드 순차 구조. 지침 v4.0(3차 개정본) 반영 완료.
- **슬라이드 완성본은 아직 프로토타입 단계** — "지침수정테스트"라는 고정 샘플 데이터로 만든 정적 HTML이고, 실제 손님 리포트마다 자동으로 이 포맷을 생성해주는 파이프라인 연동은 아직 안 됨. 다음 단계는:
  1. 이 슬라이드 디자인을 실제 파이프라인 출력(매번 다른 고객의 리포트 텍스트)에 맞춰 **동적으로 렌더링**하는 스크립트로 만들기 (예: n8n에서 검증 완료된 텍스트 + JSON 데이터를 받아 이 HTML 구조를 채워 넣고, 헤드리스 브라우저로 PDF 변환)
  2. PDF export 파이프라인 구축 (print-to-PDF, 실제 파일 저장/전달 경로 설계)
  3. 대운 타임라인 같은 추가 데이터 시각화 슬라이드는 아직 안 만듦(오행·세운 2종만 있음) — 필요하면 추가
- `.claude/launch.json`에 `wf-json`(포트 8619, 스크래치패드 서빙용 임시 서버) 엔트리가 남아있음 — 로컬 HTML 미리보기용, 필요 없어지면 정리해도 됨.

## 8. 아티팩트 링크 모음
- 세로형 리포트 미리보기: https://claude.ai/code/artifact/ef9113d1-232b-4fa7-92de-78d02b0132bb
- 가로형 8장 프로토타입: https://claude.ai/code/artifact/eea51152-6ff6-4e9a-8585-cedd9f92bf8a
- 가로형 49페이지 완성본: https://claude.ai/code/artifact/3d7c4666-5912-4043-8ab6-263d60de35c8
